<UserControl x:Class="MarkdownMkII.Views.ShortcutSettings" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:vm="using:MarkdownMkII.ViewModels">
    <StackPanel Spacing="12">
        <TextBlock x:Uid="ShortcutIntro" TextWrapping="Wrap"/>
        <Grid ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" x:Uid="ShortcutSearchBox" TextChanged="OnSearch"/>
            <Button Grid.Column="1" x:Uid="ShortcutResetAllButton" Click="OnResetAll"/>
        </Grid>
        <ListView x:Name="CommandsList" SelectionMode="None" MaxHeight="580" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle><Style TargetType="ListViewItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="8"/></Style></ListView.ItemContainerStyle>
            <ListView.ItemTemplate><DataTemplate x:DataType="vm:PaletteCommand">
                <Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="120"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Spacing="2"><TextBlock Text="{x:Bind Label}" TextWrapping="Wrap"/><TextBlock Text="{x:Bind Category}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/></StackPanel>
                    <TextBlock Grid.Column="1" Text="{x:Bind Shortcut}" VerticalAlignment="Center" FontFamily="Consolas"/>
                    <Button Grid.Column="2" x:Uid="ShortcutEditButton" Tag="{x:Bind}" Click="OnEdit"/>
                    <Button Grid.Column="3" x:Uid="ShortcutResetButton" Tag="{x:Bind}" Click="OnReset"><SymbolIcon Symbol="Undo"/></Button>
                </Grid>
            </DataTemplate></ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
